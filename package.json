{
  "name": "bbp",
  "private": true,
  "scripts": {
    "watch:sass": "npx sass --watch bbp/static_src/scss/main.scss bbp/static_build/css/main.css",
    "watch:browser-sync": "npx browser-sync start --proxy localhost --watch --serve-static bbp/static_build",
    "dev": "npx run-p watch:*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bcdickinson/bbp.git"
  },
  "devDependencies": {
    "browser-sync": "^2.26.3",
    "npm-run-all": "^4.1.5",
    "sass": "^1.16.1",
    "sass-lint": "^1.12.1"
  },
  "dependencies": {}
}
