{
  "name": "bbp",
  "private": true,
  "scripts": {
    "sass-dev": "sass --embed-sources --embed-source-map bbp/static_src/scss/main.scss bbp/static_build/css/main.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bcdickinson/bbp.git"
  },
  "devDependencies": {
    "browser-sync": "^2.26.3",
    "npm-run-all": "^4.1.5",
    "sass": "^1.16.1"
  }
}
