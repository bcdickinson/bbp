{
  "name": "bbp",
  "private": true,
  "scripts": {
    "watch:sass": "npx sass --embed-sources --watch bbp/static_src/scss/main.scss bbp/static_build/css/main.css",
    "watch:browser-sync": "npx browser-sync start --no-open --proxy localhost --files \"bbp/static_build/css/*.css\" \"bbp/public/*\" \"bbp/**/*.html\" \"bbp/**/*.py\"",
    "watch:server": "docker-compose run --service-ports -v $(pwd):/app -e DJANGO_SETTINGS_MODULE=bbp.settings.dev web ./manage.py runserver --nostatic 0.0.0.0:8000",
    "start": "npx run-p watch:*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bcdickinson/bbp.git"
  },
  "devDependencies": {
    "browser-sync": "^2.26.3",
    "npm-run-all": "^4.1.5",
    "sass": "^1.16.1",
    "sass-lint": "^1.12.1"
  },
  "dependencies": {}
}
